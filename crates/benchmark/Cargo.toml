[package]
name = "zk-jam-benchmark"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
zk-jam-refine-interface = { path = "../refine-interface" }
