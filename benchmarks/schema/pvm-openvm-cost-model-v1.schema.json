{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "pvm-openvm-cost-model-v1",
  "type": "object",
  "required": ["schema_version", "environment", "workloads"],
  "properties": {
    "schema_version": {"const": "pvm-openvm-cost-model-v1"},
    "environment": {"type": "object"},
    "proof_work_definition": {"type": "string"},
    "workloads": {"type": "array", "minItems": 1}
  }
}
