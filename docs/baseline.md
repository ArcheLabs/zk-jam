# M2.0 baseline

- zk-jam commit at implementation start: `9b652f0` (`feat: add ZkRefine Smoke M0 interface`)
- Jambda reference commit: `b850a45` (`feat: add Jambda ZkRefine adapter`)
- OpenVM: `v2.0.1`, revision `b820b25baab6c5d9b055f64e0286b6b1058e707c`
- Known baseline issue: `.agent/JAMBDA_ZK_INTEGRATION_CONSTRAINTS.md` was not present in the supplied checkout, so the M2 spike follows the attached M2 specification and leaves Jambda untouched.
- Publication benchmark guest toolchain: `nightly-2026-01-18`; generic `nightly` is development-only.

This baseline records integration state only. It is not a PVM or Native proving baseline.
